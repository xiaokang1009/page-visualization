interface ScrollerProps {
  scrollerHeight?: number
  isDown?: boolean
  el?: Element
  scrollerEl?: Element
}

const Scroller = ({ scrollerHeight, isDown = true, el }: ScrollerProps) => {}

export default Scroller
